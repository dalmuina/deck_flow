// Compose stability configuration for :feature-card.
// ViewModel instances are retained across recomposition by the ViewModelStore,
// so it's safe to treat them as stable even though the compiler can't verify
// it structurally. See https://developer.android.com/develop/ui/compose/performance/stability/fix
com.dalmuina.feature.card.presentation.session.CardViewModel
com.dalmuina.feature.card.presentation.timer.TimerViewModel
