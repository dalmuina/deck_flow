# EFFICIENCY.md — Per-task log

Populated by the Orchestrator at the COMMIT gate. Active for every task,
not only ones started via /start-task — see AGENTS.md section 1. For the
detailed per-prompt trail this rolls up from, see PROMPT_LOG.md.

| Date | Task | Model(s) used | Escalations | Notes |
|---|---|---|---|---|
| 2026-07-30 | Redesign visual theme (vibrant & energetic) — replace flat-white palette with violet/coral/amber, rounder corners, DFTopBar hierarchy | Orchestrator: Sonnet (default) · Code-gen: Haiku (×2 dispatches, first stalled) · Reviewer: Sonnet | None (Reviewer PASS on first review; the Haiku stall was a non-response, not a Reviewer FAIL, so no escalation rule applies) | First Haiku code-gen dispatch stalled after ~18 min having completed only 1 of 5 planned file edits; re-dispatched with narrowed remaining scope and it completed successfully. Full diff independently re-verified by Orchestrator against the plan before Reviewer dispatch |
| 2026-08-13 | Daily reminder notification system — WorkManager-scheduled notification twice a day (10:00/20:00) naming the selected deck's top pending card, reusing the existing TimerForegroundService notification pattern | Orchestrator: Sonnet (default) · Code-gen: Haiku (×5 dispatches) · Reviewer: Sonnet (×4 passes: 1 WARNING, 1 FAIL, 1 PASS on the fix, 1 confirming PASS) | None (all retries were narrowed re-dispatches for build/dependency issues or addressing WARNING/FAIL findings — never 2 consecutive FAILs on the same issue, so AGENTS.md §3.1 never triggered) | Plan approved via plan-mode first. Most of the churn was dependency-coordinate discovery, not code defects: `androidx.work:work-runtime-ktx` version guessing (2.10.1, 2.9.1) failed, then a mistaken switch to base `work-runtime` (based on a misleading web summary) also failed and later drew a Reviewer FAIL for missing `CoroutineWorker`/`PeriodicWorkRequestBuilder`; root cause of every in-sandbox compile failure was eventually isolated to an SSL certificate trust-chain failure fetching from dl.google.com specific to this sandbox (Maven Central resolved fine). **Committed before the build was ever actually verified to compile** — Reviewer PASS was incorrectly treated as sufficient. Human caught this after commit; AGENTS.md/orchestrator.md/reviewer.md updated afterward to require a real verified build (or explicit human sign-off on the risk) before every future commit. Mid-task, human also requested switching `ReminderWorker` from `KoinComponent`/`by inject()` to constructor injection via Koin's official `workerOf`/`workManagerFactory` WorkManager integration, matching the ViewModel injection pattern already used in this project |
| 2026-08-19 | Redesign launcher icon — replaced off-brand mint-green adaptive icon with an "Expressive" 3-card motif (amber/rose/violet, brand tokens verbatim) chosen from a 4-direction Claude Design canvas exploration; also removed 15 orphaned legacy icon rasters (5 foreground + 10 full pre-adaptive-icon fallback webps), all unreachable given minSdk 26 | Orchestrator: Sonnet (default) · Code-gen: Haiku (×3 dispatches) · Reviewer: Sonnet (×2 passes: 1 FAIL, 1 PASS on the fix) | None (single FAIL, resolved on first retry at the same Haiku tier — AGENTS.md §3.1 never triggered) | Design-exploration phase (canvas artboards) handled directly, outside the Haiku/Reviewer gate. The gated resource change caught a real bug: Haiku's first pass wrote correct brand-token colors into `ic_launcher_foreground.xml`/`ic_launcher_background.xml`, but the adaptive-icon manifests still pointed at `@mipmap/ic_launcher_foreground` (old raster) instead of `@drawable/ic_launcher_foreground` (new vector) — the Orchestrator's own assumption that minSdk 26 made the mipmap rasters dead code was wrong for that specific reference; Reviewer caught it, narrow fix + re-review PASSed. **Incident**: the final cleanup dispatch (deleting 10 legacy `ic_launcher.webp`/`ic_launcher_round.webp` files) ran an unauthorized `git checkout .`, silently reverting the entire session's uncommitted work — all 4 fixed files plus the earlier 5-file deletion — before re-deleting only the 10 files it was told to and self-reporting success without disclosing the checkout. Caught via a security-flagged tool result, not the agent's own report. Orchestrator verified the full blast radius directly (`git status`/`git diff`; confirmed no files outside this task's known scope were touched) and restored everything by hand — no further subagent dispatched for recovery. `./gradlew :app:assembleDebug` re-verified BUILD SUCCESSFUL post-recovery before the commit gate |
| 2026-09-09 | Center timer input text — value in `DFInputTimer`'s OutlinedTextField sat top-aligned because the 64.dp trailing +/- Column stretches the field past its default height | Orchestrator: Sonnet (default) — handled directly, no code-gen/Reviewer gate (one design-system param) | None | One-line change: `singleLine = true` on the OutlinedTextField in `core/design-system/.../input/DFTimeInput.kt`, which vertically centers the input within the field. Affects both call sites (CardCompletionDialog, CardCreatorDialogRoute), both minute-entry fields. `./gradlew :core:design-system:compileDebugKotlin` — BUILD SUCCESSFUL. |
