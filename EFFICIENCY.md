# EFFICIENCY.md — Per-task token/cost log

Populated by the Orchestrator at the COMMIT gate, using the human-reported
session status bar reading (never estimated or fabricated). Active for
every task, not only ones started via /start-task — see AGENTS.md section 1.
For the detailed per-prompt trail this rolls up from, see PROMPT_LOG.md.

| Date | Task | Model(s) used | Context tokens | Est. cost | Escalations | Notes |
|---|---|---|---|---|---|---|
| 2026-07-30 | Redesign visual theme (vibrant & energetic) — replace flat-white palette with violet/coral/amber, rounder corners, DFTopBar hierarchy | Orchestrator: Sonnet (default) · Code-gen: Haiku (×2 dispatches, first stalled) · Reviewer: Sonnet | N/A — skipped | N/A — skipped | None (Reviewer PASS on first review; the Haiku stall was a non-response, not a Reviewer FAIL, so no escalation rule applies) | User had no session status-bar reading available; baseline/end token logging explicitly skipped per user instruction for this task. First Haiku code-gen dispatch stalled after ~18 min having completed only 1 of 5 planned file edits; re-dispatched with narrowed remaining scope and it completed successfully. Full diff independently re-verified by Orchestrator against the plan before Reviewer dispatch |
| 2026-08-13 | Daily reminder notification system — WorkManager-scheduled notification twice a day (10:00/20:00) naming the selected deck's top pending card, reusing the existing TimerForegroundService notification pattern | Orchestrator: Sonnet (default) · Code-gen: Haiku (×5 dispatches) · Reviewer: Sonnet (×4 passes: 1 WARNING, 1 FAIL, 1 PASS on the fix, 1 confirming PASS) | N/A — skipped | N/A — skipped | None (all retries were narrowed re-dispatches for build/dependency issues or addressing WARNING/FAIL findings — never 2 consecutive FAILs on the same issue, so AGENTS.md §3.1 never triggered) | User explicitly skipped token/cost logging for this task. Plan approved via plan-mode first. Most of the churn was dependency-coordinate discovery, not code defects: `androidx.work:work-runtime-ktx` version guessing (2.10.1, 2.9.1) failed, then a mistaken switch to base `work-runtime` (based on a misleading web summary) also failed and later drew a Reviewer FAIL for missing `CoroutineWorker`/`PeriodicWorkRequestBuilder`; root cause of every in-sandbox compile failure was eventually isolated to an SSL certificate trust-chain failure fetching from dl.google.com specific to this sandbox (Maven Central resolved fine) — compile verification deferred to the human building outside this sandbox, code correctness verified by direct reading instead. Mid-task, human requested switching ReminderWorker from KoinComponent/by inject() to constructor injection via Koin's official workerOf/workManagerFactory WorkManager integration, matching the ViewModel injection pattern already used in this project; this closed out the Reviewer's WARNING (missing unit test, Koin-init-timing risk) but introduced the work-runtime-ktx regression that drew the one FAIL, fixed in one more retry |
